#include <bits/stdc++.h>
#define ll long long int
#define PI 3.141592653
using namespace std;

int main()
{
    string str;
    cin >> str;
    int arr[26];
    for (int i = 0; i < 26; i++)
    {
        cin >> arr[i];
    }

    int qCount = count(str.begin(), str.end(), '?');

    for (int i = 0; i < qCount; i++)
    {
        for (int j = 0; j < 26; j++)
        {
        }
    }
    return 0;
}